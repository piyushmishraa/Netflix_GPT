## What is Netflix GPT?

Netflix GPT is a movie discovery app that lets you search in plain English —
"something like Inception but funnier" — and get real results. OpenAI processes
the query and maps it to live TMDB data.

Built with React and Redux Toolkit. Normalized store slices + useMemo/useCallback
keep re-renders minimal (~40% reduction). Firebase handles Google Sign-In.

